The current best checkpoint is a compact GPT that stays under the 2,000,000 parameter cap while evaluating successfully on the provided dev text. Its saved config is `vocab_size=1024`, `block_size=128`, `n_layer=5`, `n_head=4`, `n_embd=160`, `dropout=0.05`, and `tie_weights=True`, for a total of 1,731,040 parameters. The checkpoint was trained for 2,000 steps and scores 2.0934 bpb on `dev_eval.txt` with the documented evaluation command. The important practical point is that `evaluate.py` must be run from the `starter/` directory so the relative paths to the checkpoint and data file resolve correctly.